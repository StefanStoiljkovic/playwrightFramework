{
  "name": "playwright_framework_tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm ci && npm run install-playwright",
    "install-playwright": "npx playwright install && npx playwright install-deps",
    "test" : "set Test_Name=%1 && npx playwright test -g \"%Test_Name%\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5",
    "allure-playwright": "^3.0.8",
    "cross-env": "^7.0.3"
  }
}
